<Window x:Class="TNPASerch.View.AddTextView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TNPASerch.View"
        mc:Ignorable="d"
        Title="AddTextView" Height="150" Width="800" Topmost="True" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.CommandBindings>
        <CommandBinding Command="Open" 
                        Executed="CommandOkBinding_Executed"></CommandBinding>
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.8*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Column="0" Grid.ColumnSpan="4" Margin="8" Text="{Binding TextValue}"></TextBox>
        <Button Grid.Column="2" Grid.Row="1" Margin="8" Command="{Binding OkCommand}" IsDefault="True">Ok</Button>
        <Button Grid.Column="3" Grid.Row="1" Margin="8" Command="{Binding CancelCommand}" IsCancel="True">Отмена</Button>
    </Grid>
</Window>
